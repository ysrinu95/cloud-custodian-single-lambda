policies:
- name: elastic-ips-unused
  resource: aws.elastic-ip
  description: Find Elastic IP addresses that are allocated but not associated with
    any instance. These incur charges without providing value.
  filters:
  - AssociationId: absent
  actions:
  - type: tag
    key: CostOptimization
    value: UnusedEIP
